import Login from "@src/components/Login/Login";


const Auth = () => {
    return (
        <section
            style={{
                width: '100vw',
                height: '100vh',
                position: 'relative',
            }}
        >
            <Login/>
        </section>
    );
}

export default Auth;